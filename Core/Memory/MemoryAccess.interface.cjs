/**
 * Explicit contract used by scanner and freezer implementations.
 * Implementations must provide read(pid,address,size), write(pid,address,bytes),
 * readInt32(address), and writeInt32(address,value).
 */
module.exports = {};
